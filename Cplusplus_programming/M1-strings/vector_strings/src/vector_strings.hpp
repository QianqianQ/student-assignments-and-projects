#ifndef AALTO_ELEC_CPP_VECTOR_STRINGS
#define AALTO_ELEC_CPP_VECTOR_STRINGS

#include <vector>
#include <string>

void cmdReader();
void adder(std::vector<std::string>&);
void remover(std::vector<std::string>&);
void printer(std::vector<std::string>&);

#endif

